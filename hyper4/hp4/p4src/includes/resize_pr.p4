/*
David Hancock
FLUX Research Group
University of Utah
dhancock@cs.utah.edu

HyPer4: A P4 Program to Run Other P4 Programs

resize_pr.p4: Resize parsed representation prior to copying extracted.data to
              ext and deparsing
*/

action a_resize_pr_01() { pop(ext, 100); push(ext, 1); }
action a_resize_pr_02() { pop(ext, 100); push(ext, 2); }
action a_resize_pr_03() { pop(ext, 100); push(ext, 3); }
action a_resize_pr_04() { pop(ext, 100); push(ext, 4); }
action a_resize_pr_05() { pop(ext, 100); push(ext, 5); }
action a_resize_pr_06() { pop(ext, 100); push(ext, 6); }
action a_resize_pr_07() { pop(ext, 100); push(ext, 7); }
action a_resize_pr_08() { pop(ext, 100); push(ext, 8); }
action a_resize_pr_09() { pop(ext, 100); push(ext, 9); }
action a_resize_pr_10() { pop(ext, 100); push(ext, 10); }
action a_resize_pr_11() { pop(ext, 100); push(ext, 11); }
action a_resize_pr_12() { pop(ext, 100); push(ext, 12); }
action a_resize_pr_13() { pop(ext, 100); push(ext, 13); }
action a_resize_pr_14() { pop(ext, 100); push(ext, 14); }
action a_resize_pr_15() { pop(ext, 100); push(ext, 15); }
action a_resize_pr_16() { pop(ext, 100); push(ext, 16); }
action a_resize_pr_17() { pop(ext, 100); push(ext, 17); }
action a_resize_pr_18() { pop(ext, 100); push(ext, 18); }
action a_resize_pr_19() { pop(ext, 100); push(ext, 19); }
action a_resize_pr_20() { pop(ext, 100); push(ext, 20); }
action a_resize_pr_21() { pop(ext, 100); push(ext, 21); }
action a_resize_pr_22() { pop(ext, 100); push(ext, 22); }
action a_resize_pr_23() { pop(ext, 100); push(ext, 23); }
action a_resize_pr_24() { pop(ext, 100); push(ext, 24); }
action a_resize_pr_25() { pop(ext, 100); push(ext, 25); }
action a_resize_pr_26() { pop(ext, 100); push(ext, 26); }
action a_resize_pr_27() { pop(ext, 100); push(ext, 27); }
action a_resize_pr_28() { pop(ext, 100); push(ext, 28); }
action a_resize_pr_29() { pop(ext, 100); push(ext, 29); }
action a_resize_pr_30() { pop(ext, 100); push(ext, 30); }
action a_resize_pr_31() { pop(ext, 100); push(ext, 31); }
action a_resize_pr_32() { pop(ext, 100); push(ext, 32); }
action a_resize_pr_33() { pop(ext, 100); push(ext, 33); }
action a_resize_pr_34() { pop(ext, 100); push(ext, 34); }
action a_resize_pr_35() { pop(ext, 100); push(ext, 35); }
action a_resize_pr_36() { pop(ext, 100); push(ext, 36); }
action a_resize_pr_37() { pop(ext, 100); push(ext, 37); }
action a_resize_pr_38() { pop(ext, 100); push(ext, 38); }
action a_resize_pr_39() { pop(ext, 100); push(ext, 39); }
action a_resize_pr_40() { pop(ext, 100); push(ext, 40); }
action a_resize_pr_41() { pop(ext, 100); push(ext, 41); }
action a_resize_pr_42() { pop(ext, 100); push(ext, 42); }
action a_resize_pr_43() { pop(ext, 100); push(ext, 43); }
action a_resize_pr_44() { pop(ext, 100); push(ext, 44); }
action a_resize_pr_45() { pop(ext, 100); push(ext, 45); }
action a_resize_pr_46() { pop(ext, 100); push(ext, 46); }
action a_resize_pr_47() { pop(ext, 100); push(ext, 47); }
action a_resize_pr_48() { pop(ext, 100); push(ext, 48); }
action a_resize_pr_49() { pop(ext, 100); push(ext, 49); }
action a_resize_pr_50() { pop(ext, 100); push(ext, 50); }
action a_resize_pr_51() { pop(ext, 100); push(ext, 51); }
action a_resize_pr_52() { pop(ext, 100); push(ext, 52); }
action a_resize_pr_53() { pop(ext, 100); push(ext, 53); }
action a_resize_pr_54() { pop(ext, 100); push(ext, 54); }
action a_resize_pr_55() { pop(ext, 100); push(ext, 55); }
action a_resize_pr_56() { pop(ext, 100); push(ext, 56); }
action a_resize_pr_57() { pop(ext, 100); push(ext, 57); }
action a_resize_pr_58() { pop(ext, 100); push(ext, 58); }
action a_resize_pr_59() { pop(ext, 100); push(ext, 59); }
action a_resize_pr_60() { pop(ext, 100); push(ext, 60); }
action a_resize_pr_61() { pop(ext, 100); push(ext, 61); }
action a_resize_pr_62() { pop(ext, 100); push(ext, 62); }
action a_resize_pr_63() { pop(ext, 100); push(ext, 63); }
action a_resize_pr_64() { pop(ext, 100); push(ext, 64); }
action a_resize_pr_65() { pop(ext, 100); push(ext, 65); }
action a_resize_pr_66() { pop(ext, 100); push(ext, 66); }
action a_resize_pr_67() { pop(ext, 100); push(ext, 67); }
action a_resize_pr_68() { pop(ext, 100); push(ext, 68); }
action a_resize_pr_69() { pop(ext, 100); push(ext, 69); }
action a_resize_pr_70() { pop(ext, 100); push(ext, 70); }
action a_resize_pr_71() { pop(ext, 100); push(ext, 71); }
action a_resize_pr_72() { pop(ext, 100); push(ext, 72); }
action a_resize_pr_73() { pop(ext, 100); push(ext, 73); }
action a_resize_pr_74() { pop(ext, 100); push(ext, 74); }
action a_resize_pr_75() { pop(ext, 100); push(ext, 75); }
action a_resize_pr_76() { pop(ext, 100); push(ext, 76); }
action a_resize_pr_77() { pop(ext, 100); push(ext, 77); }
action a_resize_pr_78() { pop(ext, 100); push(ext, 78); }
action a_resize_pr_79() { pop(ext, 100); push(ext, 79); }
action a_resize_pr_80() { pop(ext, 100); push(ext, 80); }
action a_resize_pr_81() { pop(ext, 100); push(ext, 81); }
action a_resize_pr_82() { pop(ext, 100); push(ext, 82); }
action a_resize_pr_83() { pop(ext, 100); push(ext, 83); }
action a_resize_pr_84() { pop(ext, 100); push(ext, 84); }
action a_resize_pr_85() { pop(ext, 100); push(ext, 85); }
action a_resize_pr_86() { pop(ext, 100); push(ext, 86); }
action a_resize_pr_87() { pop(ext, 100); push(ext, 87); }
action a_resize_pr_88() { pop(ext, 100); push(ext, 88); }
action a_resize_pr_89() { pop(ext, 100); push(ext, 89); }
action a_resize_pr_90() { pop(ext, 100); push(ext, 90); }
action a_resize_pr_91() { pop(ext, 100); push(ext, 91); }
action a_resize_pr_92() { pop(ext, 100); push(ext, 92); }
action a_resize_pr_93() { pop(ext, 100); push(ext, 93); }
action a_resize_pr_94() { pop(ext, 100); push(ext, 94); }
action a_resize_pr_95() { pop(ext, 100); push(ext, 95); }
action a_resize_pr_96() { pop(ext, 100); push(ext, 96); }
action a_resize_pr_97() { pop(ext, 100); push(ext, 97); }
action a_resize_pr_98() { pop(ext, 100); push(ext, 98); }
action a_resize_pr_99() { pop(ext, 100); push(ext, 99); }
action a_resize_pr_100() { pop(ext, 100); push(ext, 100); }

table t_resize_pr {
  reads {
    parse_ctrl.numbytes : exact;
  }
  actions {
    a_resize_pr_01;
    a_resize_pr_02;
    a_resize_pr_03;
    a_resize_pr_04;
    a_resize_pr_05;
    a_resize_pr_06;
    a_resize_pr_07;
    a_resize_pr_08;
    a_resize_pr_09;
    a_resize_pr_10;
    a_resize_pr_11;
    a_resize_pr_12;
    a_resize_pr_13;
    a_resize_pr_14;
    a_resize_pr_15;
    a_resize_pr_16;
    a_resize_pr_17;
    a_resize_pr_18;
    a_resize_pr_19;
    a_resize_pr_20;
    a_resize_pr_21;
    a_resize_pr_22;
    a_resize_pr_23;
    a_resize_pr_24;
    a_resize_pr_25;
    a_resize_pr_26;
    a_resize_pr_27;
    a_resize_pr_28;
    a_resize_pr_29;
    a_resize_pr_30;
    a_resize_pr_31;
    a_resize_pr_32;
    a_resize_pr_33;
    a_resize_pr_34;
    a_resize_pr_35;
    a_resize_pr_36;
    a_resize_pr_37;
    a_resize_pr_38;
    a_resize_pr_39;
    a_resize_pr_40;
    a_resize_pr_41;
    a_resize_pr_42;
    a_resize_pr_43;
    a_resize_pr_44;
    a_resize_pr_45;
    a_resize_pr_46;
    a_resize_pr_47;
    a_resize_pr_48;
    a_resize_pr_49;
    a_resize_pr_50;
    a_resize_pr_51;
    a_resize_pr_52;
    a_resize_pr_53;
    a_resize_pr_54;
    a_resize_pr_55;
    a_resize_pr_56;
    a_resize_pr_57;
    a_resize_pr_58;
    a_resize_pr_59;
    a_resize_pr_60;
    a_resize_pr_61;
    a_resize_pr_62;
    a_resize_pr_63;
    a_resize_pr_64;
    a_resize_pr_65;
    a_resize_pr_66;
    a_resize_pr_67;
    a_resize_pr_68;
    a_resize_pr_69;
    a_resize_pr_70;
    a_resize_pr_71;
    a_resize_pr_72;
    a_resize_pr_73;
    a_resize_pr_74;
    a_resize_pr_75;
    a_resize_pr_76;
    a_resize_pr_77;
    a_resize_pr_78;
    a_resize_pr_79;
    a_resize_pr_80;
    a_resize_pr_81;
    a_resize_pr_82;
    a_resize_pr_83;
    a_resize_pr_84;
    a_resize_pr_85;
    a_resize_pr_86;
    a_resize_pr_87;
    a_resize_pr_88;
    a_resize_pr_89;
    a_resize_pr_90;
    a_resize_pr_91;
    a_resize_pr_92;
    a_resize_pr_93;
    a_resize_pr_94;
    a_resize_pr_95;
    a_resize_pr_96;
    a_resize_pr_97;
    a_resize_pr_98;
    a_resize_pr_99;
    a_resize_pr_100;
    _no_op;
  }
}
